# M006: Provider Selection UX and End-to-End Codex Launch — Context Brief\n\n## Problem Statement\n\nThe provider-neutral architecture exists at the contract level (AgentProvider, AgentRunner protocols; Claude and Codex adapters; governed-artifact renderers), but the entire live runtime is hardwired to Claude. A user cannot select Codex. The product brand says \"Sandboxed Claude CLI.\" There is no Codex container image, no Codex runner, and no provider-aware session/container/volume identity.\n\n## Seven Requirement Areas + Five Execution Constraints\n\n### Requirement areas (from user direction)\n1. **Provider identity in runtime/session/container** — provider in container name, volume name, session identity, resume identity\n2. **Quick Resume provider-aware** — filter by workspace+team+provider, refuse mismatch\n3. **Doctor: backend ≠ provider readiness** — separate health checks, `--provider <name>`, actionable commands\n4. **Codex image lifecycle** — Dockerfile, image contracts, operator build guidance\n5. **TUI changes explicit** — all surfaces, dashboard `a` key, \"Agent\" in interactive UX\n6. **Truthful feature gating** — Claude-only features hidden/gated under Codex\n7. **Migration planning** — backward-compat defaults for config and sessions\n\n### Five execution constraints (D028)\n1. **Policy validation** — provider selection validated against org/team policy, with ProviderNotAllowedError\n2. **Request-scoped resolution** — shared infra cached, provider-specific adapters per-invocation\n3. **Machine-readable outputs** — provider_id in dry-run JSON, support bundle, audit events, session/container list --json\n4. **Standardized image build UX** — doctor and start print `docker build -t scc-agent-{provider}:latest images/scc-agent-{provider}/`\n5. **Coexistence test** — prove Claude and Codex coexist on same workspace without collision\n\n## Hotspot Files\n\n| File | Gap |\n|------|-----|\n| `adapters/oci_sandbox_runtime.py` | AGENT_NAME, SANDBOX_DATA_VOLUME, _container_name() all Claude-shaped |\n| `core/constants.py` | AGENT_NAME, AGENT_CONFIG_DIR, SANDBOX_DATA_VOLUME, SANDBOX_IMAGE, CREDENTIAL_PATHS, OAUTH_CREDENTIAL_KEY all Claude-specific |\n| `core/image_contracts.py` | Only SCC_CLAUDE_IMAGE defined |\n| `ports/session_models.py` | SessionRecord has no provider_id |\n| `ui/quick_resume.py` | Filters by workspace+team only, not provider |\n| `ui/branding.py` | \"Sandboxed Claude CLI\" hardcoded |\n| `theme.py` | \"Sandboxed Claude CLI\" in header strings |\n| `commands/launch/render.py` | \"Launching Claude Code\" hardcoded |\n| `commands/launch/flow.py` | Docstring says \"Start Claude Code\", no --provider flag |\n| `ui/dashboard/_dashboard.py` | No provider display, p=profile_menu (a is free) |\n| `commands/worktree/container_commands.py` | \"Claude Code sandboxes\" in help/output |\n| `sessions.py` | Claude-specific get_claude_sessions_dir, get_claude_recent_sessions |\n| `doctor/render.py` | \"Ready to run Claude Code\" hardcoded |\n| `application/start_session.py` | SCC_CLAUDE_IMAGE_REF hardcoded for OCI image |\n| `bootstrap.py` | lru_cache bakes ClaudeAgentProvider + ClaudeAgentRunner at first call |\n| `docker/credentials.py` | SANDBOX_DATA_VOLUME×9, OAUTH_CREDENTIAL_KEY Claude-specific |\n| `presentation/json/launch_json.py` | dry-run envelope has no provider field |\n| `presentation/json/sessions_json.py` | session list has no provider field |\n| `application/support_bundle.py` | no provider field in bundle |\n\n## Key Decisions\n\n- **D005:** V1 supports Claude and Codex only\n- **D019:** Claude and Codex surfaces are intentionally asymmetric\n- **D026:** Provider selection: --provider > selected_provider > default; dispatch in bootstrap\n- **D027:** ProviderRuntimeSpec separate from CapabilityProfile\n- **D028:** Five execution constraints: policy validation, request-scoped resolution, machine-readable outputs, standardized image commands, coexistence test\n\n## Scope Boundaries\n\n### In scope\n- Provider resolution (per-invocation, policy-validated)\n- ProviderRuntimeSpec model\n- CodexAgentRunner + Codex Dockerfile + image contracts\n- Provider-aware container naming, volume naming, session identity\n- Provider-aware Quick Resume filtering and mismatch handling\n- Provider-aware doctor readiness (separate from backend health)\n- Provider-aware TUI (all surfaces, dashboard `a` key)\n- Machine-readable provider output (dry-run JSON, support bundle, session/container list --json)\n- Truthful feature gating (Claude-only features gated under Codex)\n- Migration handling for pre-M006 config and sessions\n- Typed error handling for all provider error scenarios\n- Coexistence test for same-workspace multi-provider\n- Standardized image build commands in doctor/start\n\n### Out of scope\n- Third provider support\n- Live Codex auth flow (API-key based, separate from Claude OAuth)\n- Codex resume support (not supported per CapabilityProfile)\n- Docker Desktop sandbox template for Codex (OCI backend is primary)\n- Image build/push CI pipeline (operator builds locally, same as M003)\n- scc images build/list command (v1 uses direct docker build)\n"}
</invoke>