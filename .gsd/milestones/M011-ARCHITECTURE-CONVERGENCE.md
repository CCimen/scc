# M011 - Architecture Convergence And Interaction Surface Slimming

## Purpose

Make SCC easier to maintain, explain, and extend before adding another runtime mode. This milestone is about convergence: one owner per concept, slimmer launch/setup/dashboard surfaces, fewer pre-production compatibility paths, clearer typed boundaries, deterministic tests, and docs that match source behavior.

S01 is inventory only. It changes no `src/**` or `tests/**` files. The S01 diff is limited to `.gsd/**` plus `AGENTS.md` milestone guidance.

## Source-grounded gap map

| Finding | Status | Evidence | Already-owned-by | Reuse/Evolve/Delete | Characterization or guardrail status | Next action |
|---|---|---|---|---|---|---|
| M011 scope must not collide with the M010 runtime/devcontainer forecast | Resolved in S01 | M010 previously said runtime/devcontainer should be M011; D053 makes architecture convergence M011 and moves runtime/devcontainer to M012 | `.gsd/PROJECT.md`, `.gsd/DECISIONS.md`, M010 milestone file | Reuse + record | N/A | Keep M011 focused on architecture convergence; do not implement devcontainer attachment in this milestone |
| Inventory artifacts must not recreate deleted context/roadmap sprawl | Resolved in S01 | D051 says `.gsd/PROJECT.md` is the milestone register and current/future milestone files are supplementary only | `.gsd/PROJECT.md`, this milestone file | Reuse | N/A | Keep one M011 file; do not add `M011-CONTEXT.md`, `M011-ROADMAP.md`, or a standalone owner map |
| Source file size has three warning-zone files and no failing files | Measured 2026-06-28 | `uv run pytest tests/test_file_sizes.py -q -s --no-cov`: 299 files scanned; warning zone: `setup.py` 1035, `ui/dashboard/orchestrator_handlers.py` 878, `commands/launch/flow_interactive.py` 811; fail zone: 0 | `tests/test_file_sizes.py` | Reuse guardrail | Warning 800, fail 1100 | Split only when a slice has characterization coverage and a canonical owner; do not chase files below warning threshold first |
| Function size has 11 warning-zone functions and no failing functions | Measured 2026-06-28 | `uv run pytest tests/test_function_sizes.py -q -s --no-cov`: top warnings are `commands/launch/flow.py:start` 297, `flow_interactive.py:interactive_start` 282, `commands/reset.py:reset_cmd` 274, `setup.py:run_setup_wizard` 272, `flow_session.py:_resolve_session_selection` 236 | `tests/test_function_sizes.py` | Reuse guardrail | Warning 200, fail 300; `flow.py:start` is 3 lines below the fail threshold | Use function warnings to rank slices, but preserve behavior with focused characterization tests before splitting |
| Setup remains a large TUI/composition surface | Open | `src/scc_cli/setup.py` is 1035 lines; `run_setup_wizard()` starts at line 614 and is 272 lines | `setup.py`, `setup_config.py`, `setup_ui.py`, `application/launch/start_wizard.py` | Evolve existing owners | `tests/test_setup_characterization.py`, `tests/test_setup_idempotency.py`, and auth vocabulary guardrails exist | M011/S03 should move one concrete setup responsibility into an existing owner; avoid a new wizard framework |
| Dashboard handlers mix UI actions with launch orchestration | Open | `src/scc_cli/ui/dashboard/orchestrator_handlers.py` is 878 lines; worktree/session start handlers import launch dependencies and runtime readiness internals | `ui/dashboard/*`, `application/dashboard.py`, `application/dashboard_models.py`, launch use cases | Move + evolve | `tests/test_dashboard_orchestrator_characterization.py`, `tests/test_ui_dashboard.py`, and dashboard ownership guardrails exist | M011/S04 should route dashboard start/resume through a smaller application/command use case instead of growing UI handler imports |
| Interactive launch still carries a large orchestration function | Open | `src/scc_cli/commands/launch/flow_interactive.py` is 811 lines; `interactive_start()` starts at line 352 and is 282 lines | `commands/launch/*`, `application/launch/*`, `wizard_resume.py` | Move + evolve | `tests/test_launch_flow_characterization.py`, start wizard tests, and preflight guardrails exist | M011/S04 should share one start-wizard path with dashboard/setup where possible; no second launch owner |
| `compute_effective_config.py` is below the file warning threshold but still owns critical policy decisions | Watch | Current size is 739 lines, not the stale 710 snapshot; application `EffectiveConfig` is defined in `effective_config_models.py:74` and constructed by `compute_effective_config.py:564` | Effective-config application layer | Evolve | `tests/test_compute_effective_config_characterization.py`, `tests/test_config_inheritance.py`, `tests/test_effective_context_project_policy.py`, and config explain tests exist | M011/S05 should type the most valuable decision values first; do not create a parallel `EffectiveContext` stack |
| Two `EffectiveConfig` classes created a naming/ownership ambiguity, not a semantic duplicate | Resolved in S02a | Application `EffectiveConfig` remains the org/team/project merge output at `application/effective_config_models.py:74`; marketplace team resolution is now `MarketplaceResolution` in `marketplace/resolve.py`; `sync_marketplace.py` uses `MarketplaceConfigResolver` | Effective-config application layer and marketplace resolver | Rename + guard | `tests/test_import_boundaries.py::TestEffectiveConfigOwnershipBoundary` keeps `EffectiveConfig` application-owned and prevents marketplace imports from reusing that name | Keep `resolve_effective_config()` function naming for this slice to avoid wiring churn; revisit only if it causes real caller confusion |
| Pre-production compatibility seams still exist | Open | Examples: `config.py:543-544` normalizes legacy `.scc.yaml` `mcp_servers`; `git.py` is a backward-compatible facade; `docker/launch.py`, `docker/__init__.py`, `commands/config.py`, `commands/org/__init__.py`, `commands/worktree/__init__.py`, and `ui/picker.py` keep re-export compatibility comments | Specific module owners plus `tests/test_import_boundaries.py` | Delete unless persisted data is protected | Import-boundary tests already prevent several reintroduced facades | M011/S02 should classify each seam by `protects persisted user data?`; keep only real config migrations such as `.scc.yaml` key normalization |
| Raw `dict[str, Any]` is concentrated at trust and renderer boundaries | Open | Examples include `application/compute_effective_config.py`, `application/profiles.py`, `core/personal_profiles.py`, `marketplace/render.py`, provider renderers, support bundle, and `effective_config_models.py` decision values | `ports/config_models.py`, effective-config models, provider renderers, support/audit owners | Evolve | `uv run mypy src/scc_cli` is the broad guardrail; boundary tests cover ownership | M011/S05 should reduce `Any` where it crosses policy, audit, or provider boundaries; leave local JSON parsing dicts alone until a typed owner exists |
| Test debt is mixed: platform skips are acceptable, stale feature skips and isolation xfails are not | Open | `rg` found 23 skips and 2 xfails in the full suite baseline; stale examples include `tests/test_start_dryrun.py:365`, `tests/test_session_flags.py:331`, `tests/test_session_flags.py:444`, and `tests/test_ui_integration.py:441/462` | Owning feature test files | Delete or fix | Full suite baseline is 5158 passed, 23 skipped, 2 xfailed | M011/S06 should split platform/optional dependency skips from stale behavior skips; delete dead future-feature tests or implement the missing behavior in a focused slice |
| Performance scanner produced leads, but no measured hot path yet | Watch | `complexity-optimizer` scanner flagged nested loops/sort-in-loop leads in safety tokenizer, start session, personal profile merge, marketplace compute/render, destination registry, and tests | Current source owners | Reuse + measure first | No profiling evidence yet | Only optimize paths tied to realistic org/team/project sizes or runtime latency; prefer map/set indexing where tests already prove ordering |
| Docs truth must follow source changes, not lead with narrative rewrites | Open | M010 docs were updated for project policy narrowing and ignored policy validation; M011 will change names/owners/surfaces if later slices proceed | `../scc-cli-docs`, source truth in CLI/help/schema/tests | Reuse | `bun run astro check` is the docs gate | M011/S07 should remove stale duplicate docs after source behavior changes; no new enterprise feature claims before implementation |

## Planned slices

1. S01 - Inventory and milestone reconciliation. Code-free. Establish this file, D053, current milestone order, measured size baseline, and refactor gates.
2. S02 - Compatibility and naming cleanup. S02a resolved the marketplace/application `EffectiveConfig` naming ambiguity. Remaining S02 work is to classify facades/aliases by persisted-data protection and delete pre-production re-exports where no real migration is protected.
3. S03 - Setup wizard slimming. Move one setup responsibility into an existing setup/application owner with characterization coverage.
4. S04 - Dashboard and interactive launch slimming. Reduce UI handler launch knowledge and converge repeated start/resume orchestration on existing launch/application owners.
5. S05 - Typed trust-boundary hardening. Replace high-value `Any`/dict-shaped policy or audit values with typed contracts where they cross boundaries.
6. S06 - Guardrail and test-debt cleanup. Remove stale future-feature skips, make xfails deterministic or delete them, and tighten guardrails that currently rely on prose.
7. S07 - Docs truth and duplication cleanup. Update `../scc-cli-docs` after source changes and remove stale or duplicate claims.

## Non-goals

- Do not build runtime/devcontainer interoperability in M011; it is M012.
- Do not implement SSO, SCIM, SBOM, signed policy, audit export, or a project registry in M011.
- Do not create a new `EffectiveContext` stack while the effective-config owner can evolve.
- Do not introduce new plugin systems, one-implementation interfaces, broad helper modules, or compatibility layers for imagined future users.
- Do not optimize scanner findings without profiling or realistic data-size evidence.
